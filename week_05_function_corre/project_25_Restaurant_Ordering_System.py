# Project 25 — Restaurant Ordering System
# EN: Store a menu as a dict {"pizza": 200, "burger": 120, "coke": 50}. Write add_to_order(order, item) (returns updated order list) 
# and bill(order, menu, tax=5) (returns total with tax). Run a menu loop to add items, then print an itemised bill.
# हिंदी: menu को dict {"pizza": 200, "burger": 120, "coke": 50} में रखो। add_to_order(order, item) बनाओ (updated order list return करे)
#  और bill(order, menu, tax=5) (tax के साथ total return करे)। items जोड़ने के लिए menu loop चलाओ, फिर itemised bill print करो।
# Concepts: dict lookup, */default args, list building, loop, return
# Hint: bill = sum(menu[item] for item in order), then add subtotal * tax / 100.

